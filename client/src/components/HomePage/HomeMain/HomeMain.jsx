import styles from "./HomeMain.module.scss";

const HomeMain = () => {
  return (        
    <section className={`${styles.homeMain} wrapperBlack`}>
      <div className="container">
        Reasons to choouse us
      </div>      
    </section>  
  );
};

export default HomeMain;